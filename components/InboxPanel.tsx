// components/InboxPanel.tsx
// This component has been deprecated as of the shift to a fully autonomous evolution model.
// The user approval step has been removed, making the inbox obsolete.
// The file is kept to avoid breaking any potential (though unlikely) transitive imports,
// but its functionality is gone.

import React from 'react';

export const InboxPanel = () => {
    return null; // The inbox is no longer part of the UI.
};
